
#include "GarbageHistoric.h"
#include <list>
#include <stdio.h>

int main(void){

	std::list<utils::GarbageHistoric*> ghist;
	
	for (std::list<utils::GarbageHistoric*>::iterator itHist = (ghist).begin(); itHist != (ghist).end(); itHist++)
	{
		;
	}
}
