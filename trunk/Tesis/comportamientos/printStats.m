
meanV
imeanV

desvStd
idesvStd

max(meanV)
max(imeanV)
max(desvStd)
max(idesvStd)

mw = max(contActnw)
mf = max(contActnf)
mg = max(contActng)
mr = max(contActnr)
mc = max(contActnc)
ma = max(contActna)
mu = max(contActnu)

imw = max(icontActnw);
imf = max(icontActnf);
img = max(icontActng);
imc = max(icontActnc);
imu = max(icontActnu);
imr = max(icontActnr);
ima = max(icontActna);
[imw , imf , img, imc, imu, imr, ima]


cantActnw = length(contActnw)
cantActnf = length(contActnf)
cantActng = length(contActng)
cantActnr = length(contActnr)
cantActnc = length(contActnc)
cantActna = length(contActna)
cantActnu = length(contActnu)
%mp = max(contActnp)

max([mw mf mg mr mc ma mu])

contActnw;
contActnf;
contActng;
contActnc;
contActnu;
contActnr;
contActna;

%contActnp;
%bar(contActnp)

scontActnw;
scontActnf;
scontActng;
scontActnc;
scontActnu;
scontActnr;
scontActna;

smeanV;
sdesvStd;

